/**
 * Camera QR scan screen resolving the salon.
 * Requirement: 7.2
 *
 * Placeholder - requires react-native-camera or expo-camera.
 * Flow:
 * 1. Open camera with QR reader
 * 2. On scan, call salonApi.resolveQr(payload)
 * 3. Navigate to AvailabilityScreen with salon data
 * 4. Handle malformed/unregistered QR with distinct messages
 */
export const QR_SCAN_SCREEN = 'QrScanScreen';
